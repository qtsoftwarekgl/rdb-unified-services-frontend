
services:
  rdb-urs-frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_APP_BUSINESS_REG_LOCAL_API: ${VITE_APP_BUSINESS_REG_LOCAL_API}
        VITE_APP_EXTERNAL_SERVICE_API: ${VITE_APP_EXTERNAL_SERVICE_API}
        VITE_APP_USER_MANAGEMENT_LOCAL_API: ${VITE_APP_USER_MANAGEMENT_LOCAL_API}
        VITE_APP_USER_MANAGEMENT_UAT_API: ${VITE_APP_USER_MANAGEMENT_UAT_API}
    environment:
      VITE_APP_BUSINESS_REG_LOCAL_API: ${VITE_APP_BUSINESS_REG_LOCAL_API}
      VITE_APP_EXTERNAL_SERVICE_API: ${VITE_APP_EXTERNAL_SERVICE_API}
      VITE_APP_USER_MANAGEMENT_LOCAL_API: ${VITE_APP_USER_MANAGEMENT_LOCAL_API}
      VITE_APP_USER_MANAGEMENT_UAT_API: ${VITE_APP_USER_MANAGEMENT_UAT_API}
      PORT: ${PORT}
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env