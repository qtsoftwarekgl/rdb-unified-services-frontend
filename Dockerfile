FROM node:lts

WORKDIR /usr/src/app

COPY package.json package-lock.json ./

RUN npm install

COPY . .

ARG VITE_APP_BUSINESS_REG_API
ARG VITE_APP_USER_MANAGEMENT_API
ARG VITE_APP_EXTERNAL_SERVICE_API

ENV VITE_APP_BUSINESS_REG_API=$VITE_APP_BUSINESS_REG_API
ENV VITE_APP_USER_MANAGEMENT_API=$VITE_APP_USER_MANAGEMENT_API
ENV VITE_APP_EXTERNAL_SERVICE_API=$VITE_APP_EXTERNAL_SERVICE_API

RUN npm run build

RUN npm install -g serve

EXPOSE 8080

CMD ["serve", "-s", "-l", "8080", "dist"]